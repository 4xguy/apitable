upstream backend  {
   # Railway internal networking
   # Environment variable will be injected by Railway
   server backend-server.railway.internal:8081;
   keepalive 32;
   keepalive_requests 100;
   keepalive_timeout 60s;
}
