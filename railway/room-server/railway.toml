[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"
dockerContext = "../.."

[deploy]
startCommand = "node dist/src/main.js"
healthcheckPath = "/nest/v1/actuator/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10